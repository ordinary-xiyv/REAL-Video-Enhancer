# REAL Video Enhancer 2.4.1 运行文档

> 实测环境：headless Linux 服务器（Ubuntu 22.04，无显示器），Tesla T4（15 GB VRAM，驱动 580.126.09 / CUDA 13.0），Python 3.10.12，无预装 ffmpeg。
> 实测日期：2026-09-07。以下命令均已在本机验证通过。

## 一、系统 / 硬件要求（来自 README）

|  | 最低 | 推荐 |
|--|--|--|
| CPU | 双核 x64 | 四核 x64 |
| GPU | 支持 Vulkan 1.3 的设备 | NVIDIA RTX 20 系及以上 |
| VRAM | 4 GB（NCNN 后端） | 8 GB（TensorRT 后端） |
| 内存 | 16 GB | 32 GB |
| 磁盘 | 1 GB 空闲（NCNN） | 16 GB 空闲（TensorRT） |
| 操作系统 | Windows 10/11 64bit、macOS 14+ | 现代 Linux 发行版（Ubuntu 22.04+） |

支持的 Python 版本：3.10 / 3.11 / 3.12（GUI 前端）；后端使用自带的便携 Python 3.12.9。

## 二、依赖列表

### 1. Python 依赖（GUI 前端，`requirements.txt`）

```
PySide6-essentials==6.8.0.2  requests  numpy==2.1.3  lief  psutil  py-cpuinfo
distro  pypresence  certifi  yt-dlp  validators  packaging  pillow
```

### 2. 系统依赖（本机实际安装的 apt 包）

```bash
# 创建 venv 所需
apt install python3.10-venv python3-pip

# Qt (PySide6) 运行所需的图形库（headless 环境也必须装，否则 import 即报错）
apt install libxkbcommon0 libgl1 libegl1 libglib2.0-0 libfontconfig1 libdbus-1-3 \
            libx11-6 libxext6 libxrender1 libsm6 libice6 libxcb1 libxcb-icccm4 \
            libxcb-image0 libxcb-keysyms1 libxcb-randr0 libxcb-render-util0 \
            libxcb-shape0 libxcb-sync1 libxcb-xfixes0 libxcb-xinerama0 \
            libxcb-xkb1 libxcb-cursor0 libnss3 libasound2

# NCNN/Vulkan 后端所需（缺 libvulkan.so.1 时 rife_ncnn_vulkan_python 无法 import）
apt install libvulkan1 mesa-vulkan-drivers vulkan-tools

# ffmpeg（本机没有预装；应用运行时也可用自带 ffmpeg，见下文）
apt install ffmpeg
```

### 3. 后端依赖（首次运行时下载 / 安装）

应用把「GUI 前端」和「推理后端」分开：前端用你的 venv，后端用一套**自动下载的便携 Python 3.12.9**（位于 `<仓库目录>/python/python/bin/python3`），推理库（torch/ncnn/tensorrt 等）都装进这套便携 Python 里。

首次启动时（检测到 `bin/ffmpeg` 或 `python/python/bin/python3` 不存在）会自动下载：

| 内容 | 来源 | 落点 |
|--|--|--|
| 便携 Python 3.12.9 | `github.com/TNTwise/REAL-Video-Enhancer-models` releases | `python/python/` |
| ffmpeg 静态二进制 | 同上 | `bin/ffmpeg` |
| 后端代码 | 从源码运行时使用仓库自带的 `backend/` 目录，不下载 | `backend/` |

之后在「Download」页选择安装推理后端（本质是 `便携python -m pip install`，见 `src/DownloadDeps.py`）：

- **NCNN**（Vulkan，跨 GPU）：`ncnn==1.0.20250916`、`rife-ncnn-vulkan-python-tntwise==1.4.5`、`upscale_ncnn_py==1.2.0` 等 —— 体积小，推荐在服务器上先用它。
- **PyTorch (CUDA)**：`torch`、`torchvision`、`safetensors`、`einops`、`cupy-cuda12x` 等，带 `+cuXXX` 后缀，从 `download.pytorch.org` 拉取 —— 体积数 GB。
- **TensorRT**：在 PyTorch 之上再加 `tensorrt==10.12.0.36`、`torch-tensorrt` 等 —— 体积最大，首次推理还要按输入分辨率做引擎优化。

平台无关依赖（`opencv-python-headless`、`scenedetect`、`numpy==2.2.2`、`pillow` 等）会随后端一起安装，完整清单见 `src/DownloadDeps.py` 的 `getPlatformIndependentDeps()` 与 `downloadPythonDeps()`。

## 三、基础指令

### 克隆

```bash
# 稳定版
git clone --recurse-submodules https://github.com/TNTwise/REAL-Video-Enhancer --branch 2.4.1
cd REAL-Video-Enhancer
```

### 创建 venv 并安装依赖

```bash
python3 -m venv .venv
.venv/bin/pip install -r requirements.txt
```

### 生成 UI 代码（从源码运行的必要步骤）

仓库不提交 `mainwindow.py` / `resources_rc.py`，它们由构建脚本从 `.ui` / `.qrc` 生成（见 `build.py` 的 `build_gui` / `build_resources`）。直接运行源码前必须手动生成：

```bash
.venv/bin/pyside6-uic -g python testRVEInterface.ui > mainwindow.py
.venv/bin/pyside6-rcc -g python resources.qrc > resources_rc.py
```

### 启动

```bash
# 正常桌面环境（有 X11/Wayland）
.venv/bin/python REAL-Video-Enhancer.py

# headless / 无显示器环境（offscreen，仅用于验证启动，无法交互操作界面）
QT_QPA_PLATFORM=offscreen .venv/bin/python REAL-Video-Enhancer.py
# 想自动退出可加超时：
QT_QPA_PLATFORM=offscreen timeout 45 .venv/bin/python REAL-Video-Enhancer.py
```

其他命令行参数：`--debug`（trace 模式）、`--fullscreen`、`--swap-flatpak-checks`。

## 四、实测运行结果（headless 服务器）

**结论：启动成功。** 以 `QT_QPA_PLATFORM=offscreen` 运行，主窗口完成初始化、NCNN 后端检测通过、进入 Qt 事件循环，运行 45 秒无崩溃（被 timeout 结束，退出码 124 属预期）。`log.txt` 关键输出：

```
Available Backends: ['ncnn']
NCNN GPU 0: Tesla T4
NCNN GPU 1: llvmpipe (LLVM 15.0.7, 256 bits)
REAL Video Enhancer Version: 2.4.1
RVE Backend Version: 2.4.1-dev16
```

过程中遇到的问题与解决办法：

1. **`ModuleNotFoundError: No module named 'mainwindow'`** —— UI 文件未生成，执行上面「生成 UI 代码」两步即可。
2. **`ImportError: libxkbcommon.so.0`** —— 缺 Qt 系统库，按第二节安装 apt 包。
3. **首次启动卡在依赖下载**：应用从 GitHub releases 直接下载便携 Python / ffmpeg，本机访问 GitHub release 资产（对象存储域名）完全超时。解决：用 GitHub 代理（如 `https://gh-proxy.com/<原始URL>`）手动下载后放到对应位置：
   - `python/python/` ← 解压 `cpython-3.12.9+20250317-x86_64-unknown-linux-gnu-install_only.tar.gz`
   - `bin/ffmpeg` ← 注意：release 里的静态 ffmpeg 在本机**段错误**（rc=139），改用 `apt install ffmpeg` 后 `cp /usr/bin/ffmpeg bin/ffmpeg`（版本 4.4.2，仅用于验证；正式使用建议换成更新的静态构建）。
4. **后端 pip 源**：本机直连 pypi.org 超时，已配置阿里云镜像 `~/.pip/pip.conf`，便携 Python 的 pip 同样读取该配置，安装正常。
5. **`ImportError: libvulkan.so.1`**（装 NCNN 后端后）—— `apt install libvulkan1 mesa-vulkan-drivers vulkan-tools`。
6. **便携 Python 3.12.9 中手动安装 NCNN 后端**（等同应用内 Download 页的操作）：

   ```bash
   python/python/bin/python3 -m pip install --no-warn-script-location \
     testresources==2.0.1 requests==2.32.3 opencv-python-headless==4.11.0.86 \
     pypresence==4.3.0 scenedetect==0.6.5.2 numpy==2.2.2 sympy tqdm==4.67.1 \
     typing_extensions==4.12.2 packaging==24.2 mpmath==1.3.0 pillow==11.1.0 \
     rife-ncnn-vulkan-python-tntwise==1.4.5 upscale_ncnn_py==1.2.0 ncnn==1.0.20250916
   ```

   验证后端：`cd backend && ../python/python/bin/python3 rve-backend.py --list_backends` 应输出 `Available Backends: ['ncnn']`。

### headless 环境的固有限制

- offscreen 平台下 GUI 无法交互，只能验证「启动不崩溃」；任何弹窗（如首次安装时的“是否简单安装”对话框）都会阻塞事件循环。
- 没有显示服务器，Qt 的 xcb/wayland 插件不可用，只能用 offscreen（或 minimal）平台。
- 实际视频处理由后端子进程（便携 Python + ffmpeg）完成，不依赖显示；理论上可通过命令行直接调 `backend/rve-backend.py` 做批处理，但本次只验证到应用启动。
- llvmpipe 是 Mesa 的软件 Vulkan 实现，会被 NCNN 列为 GPU 1，不要选它做推理。

## 五、常见问题（FAQ）

**Q: 启动报 `No module named 'mainwindow'`？**
A: 从源码运行必须先用 `pyside6-uic` / `pyside6-rcc` 生成 `mainwindow.py` 和 `resources_rc.py`（见第三节）。

**Q: 启动报 `libxkbcommon.so.0` / `libGL.so.1` 等找不到？**
A: 缺 Qt 系统运行库，安装第二节列出的 apt 包；headless 环境也需要安装这些库。

**Q: 首次启动一直卡在下载界面？**
A: 应用要从 GitHub releases 下载便携 Python 和 ffmpeg。网络无法访问 GitHub 资产域名时会一直卡住，可用代理手动下载放到 `python/`、`bin/` 下，应用检测到已安装（`bin/ffmpeg` + `python/python/bin/python3` 都存在）就会跳过下载。

**Q: 提示 "Please install at least one backend"？**
A: 后端检测不到任何推理框架。到 Download 页安装 NCNN 或 PyTorch；或按第四节手动用便携 Python 的 pip 安装。验证命令：`backend/rve-backend.py --list_backends`。

**Q: NCNN 报 Vulkan 错误？**
A: 先确认 `vulkaninfo` 能列出 GPU；缺 loader 就装 `libvulkan1`。Vulkan OOM 通常是显存不足或用了 llvmpipe 软渲染设备，换设备或换 PyTorch 后端。

**Q: TensorRT 首次推理特别慢 / 优化失败？**
A: TensorRT 会针对每种输入分辨率做一次引擎优化，只需一次。优化失败最常见原因是显存不足，改用 CUDA 或 NCNN 后端。

**Q: headless 服务器上能用吗？**
A: GUI 只能 offscreen 启动做验证，无法交互。要真正批量处理视频，建议直接用后端 CLI：`backend/rve-backend.py`（参数见 `backend/rve-backend.py --help`），或在有桌面的机器上运行 GUI。

**Q: 应用识别不到已安装的后端？**
A: RVE 用自带的便携 Python + pip 管理后端，不要用系统 Python 装推理库；确认包装进了 `python/python/` 这套环境里。
