# 漫剧短视频处理流水线 · 常用命令

> 场景：破坏视频内暗水印（HWater 类隐藏水印）→ 超分增强。
> 流程：**Stage 1** 轻度降噪 + 动态噪声扰动 + H.264 重编码 → **Stage 2** 微重采样 + 二次重编码 → **Stage 3** 超分增强。
> Stage 1/2 参数参照《Hwater处理》文档 Grid A 实验配置；实测环境：headless Linux，Tesla T4。
> 注意：请只处理你有权处理的视频素材。

## 路径约定

```bash
cd /root/REAL-Video-Enhancer
FFMPEG=bin/ffmpeg                                  # ffmpeg 可执行文件
BACKEND="./python/python/bin/python3 backend/rve-backend.py"   # RVE 后端 CLI
```

## Stage 1 + 2 一键脚本（推荐）

`destroy_watermark.sh`（仓库根目录）已统合 Stage 1 + 2，参数与《Hwater处理》文档一致：

```bash
./destroy_watermark.sh input.mp4 output_clean.mp4           # 文档原方案: noise=2, 缩放0.975
./destroy_watermark.sh input.mp4 output_clean.mp4 0.975 7   # 推荐: 加 ±7px 平移扰动（见下方实测）
./destroy_watermark.sh input.mp4 output_clean.mp4 0.95 12   # 更强: 缩放95% + 平移±12px
```

参数：`[输出] [pass2缩放比例=0.975] [平移像素=0]`。平移像素为可选增强项（文档原方案不含），按分辨率宽度约 1.5% 取值即可。

## Stage 1：轻度降噪 + 噪声扰动 + 重编码（手动单步）

```bash
$FFMPEG -hide_banner -y -i input.mp4 \
    -vf "setsar=1,hqdn3d=0.8:0.8:3:3,noise=alls=2:allf=t+u" \
    -c:v libx264 -preset veryslow -crf 23 -pix_fmt yuv420p \
    -c:a aac -b:a 128k -ar 44100 \
    -map_metadata -1 -movflags +faststart \
    stage1.mp4
```

参数说明（对应文档 Grid A 配置）：
- `setsar=1` — 统一像素宽高比，避免后续处理拉伸/显示异常
- `hqdn3d=0.8:0.8:3:3` — 轻度时空降噪，削弱稳定细粒度特征
- `noise=alls=2:allf=t+u` — 轻微动态噪声，改变像素分布和帧间统计特征
- `-crf 23` — **主要强度参数**：越高压缩扰动越强、水印检出率越低，画质损耗也越大
- `-preset veryslow` — 更充分的码率分配
- `-map_metadata -1` — 剥离元数据；`+faststart` — 便于分发播放

## Stage 2：微重采样 + 二次重编码（手动单步）

文档默认 `pass2_scale = 0.975`：bicubic 缩到 97.5% → lanczos 放回原尺寸，打散固定像素网格、频域对齐和局部纹理位置关系：

```bash
$FFMPEG -hide_banner -y -i stage1.mp4 \
    -vf "scale='trunc(iw*0.975/2)*2':'trunc(ih*0.975/2)*2':flags=bicubic,scale='ceil(iw/0.975/2)*2':'ceil(ih/0.975/2)*2':flags=lanczos,setsar=1" \
    -c:v libx264 -preset veryslow -crf 23 -pix_fmt yuv420p \
    -c:a copy -map_metadata -1 -movflags +faststart \
    stage2.mp4
```

> 表达式按输入分辨率自动计算，480p/720p/1080p/竖屏通用。可选增强：在两个 scale 之间插入 `crop=iw-14:ih-14:7:7`（±7px 平移），实测可将水印相关性残留从 ~36% 降至 0。

## 破坏效果实测（本机验证）

向测试视频嵌入幅度 ±3 的固定伪随机暗水印后检测相关性：

| 方案 | 水印相关性 |
|--|--|
| 无水印原片（基线） | -0.002 |
| 嵌入水印后 | **+0.184**（可检出） |
| 文档原方案（缩放 0.975，无平移） | +0.065（残留 ~36%） |
| 文档方案 + 平移 ±7px | **+0.002**（≈基线，无法检出） |

注意：文档结论基于其真实 HWater 检出率实验，本机测试用的是合成水印模型，两者互补——建议默认加平移参数，并用真实片源复检。

## Stage 3：超分增强

> 文档原方案 Stage 3 调用火山引擎 AI MediaKit 生成式超分 API（`enhance-video-generative`，`scene=short_series`，fps 统一 30，短边 >=720 → 1080p 否则 720p）。
> 本机用 RVE NCNN 本地超分替代，分辨率选择逻辑与文档一致。
> NCNN 模型只支持整数倍放大（2x/4x），非整数目标采用「**超到更高分辨率 → 缩到目标**」的超采样方式，画质反而更好。

### 480p → 720p（2x 超分后缩到 720p）

```bash
# 3.1 模型 2 倍超分：854x480 → 1708x960
$BACKEND -i stage2.mp4 -o stage3_raw.mp4 -b ncnn \
    --upscale_model models/2x_AnimeJaNai_HD_V3_Sharp1_Compact_430k \
    --ffmpeg_path $FFMPEG

# 3.2 缩到目标 1280x720
$FFMPEG -y -i stage3_raw.mp4 -vf "scale=1280:720:flags=lanczos,setsar=1" \
    -c:v libx264 -preset slow -crf 17 -pix_fmt yuv420p -c:a copy output_720p.mp4
```

### 720p → 1080p（2x 超分后缩到 1080p）

```bash
$BACKEND -i stage2.mp4 -o stage3_raw.mp4 -b ncnn \
    --upscale_model models/2x_AnimeJaNai_HD_V3_Sharp1_Compact_430k \
    --ffmpeg_path $FFMPEG

$FFMPEG -y -i stage3_raw.mp4 -vf "scale=1920:1080:flags=lanczos,setsar=1" \
    -c:v libx264 -preset slow -crf 17 -pix_fmt yuv420p -c:a copy output_1080p.mp4
```

### 1080p → 1080p（不放大，仅增强）

两种方式二选一：

```bash
# 方式 A（快）：1x 修复模型直接增强，不改分辨率
$BACKEND -i stage2.mp4 -o output_1080p.mp4 -b ncnn \
    --extra_restoration_models models/1xDeH264_RTMoSR.ncnn \
    --ffmpeg_path $FFMPEG

# 方式 B（质量更好，慢）：2x 超分后缩回 1080p（超采样抗锯齿）
$BACKEND -i stage2.mp4 -o stage3_raw.mp4 -b ncnn \
    --upscale_model models/2x_AnimeJaNai_HD_V3_Sharp1_Compact_430k \
    --ffmpeg_path $FFMPEG
$FFMPEG -y -i stage3_raw.mp4 -vf "scale=1920:1080:flags=lanczos,setsar=1" \
    -c:v libx264 -preset slow -crf 17 -pix_fmt yuv420p -c:a copy output_1080p.mp4
```

### 分辨率自动选择（与文档一致的短边逻辑）

```bash
SHORT=$(echo "$W $H" | tr ' ' '\n' | sort -n | head -1)
if [ "$SHORT" -ge 720 ]; then TARGET=1080; else TARGET=720; fi
```

## 可选：补帧 / 统一帧率

文档方案输出统一 30fps。本地可用 RIFE 补帧或 ffmpeg 直接转换：

```bash
# ffmpeg 直接转 30fps（不生成中间帧，最快）
$FFMPEG -y -i stage2.mp4 -vf "fps=30" -c:v libx264 -crf 18 -pix_fmt yuv420p -c:a copy out30.mp4

# RIFE 补帧到 2 倍帧率（动画向模型，更顺滑）
$BACKEND -i stage2.mp4 -o output_2xfps.mp4 -b ncnn \
    --interpolate_model models/rife-v4.26-heavy --interpolate_factor 2 \
    --ffmpeg_path $FFMPEG
```

超分 + 补帧可同时加在一条命令里（`--upscale_model` 与 `--interpolate_model` 同用）。

## 模型速查（漫剧/动画场景）

| 用途 | 模型 | 说明 |
|--|--|--|
| 2x 超分（主力） | `models/2x_AnimeJaNai_HD_V3_Sharp1_Compact_430k` | JaNai V3，动画首选 |
| 2x 超分（备选） | `models/2x_ModernSpanimationV2`、`models/realesr-animevideov3-x2` | 速度快 |
| 2x 超分（老动画） | `models/2x_AniSD_G6i1_SPAN_215K.ncnn` 等 3 个 | 按片源质量选高/中/低 |
| 4x 超分 | `models/realesrgan-x4plus-anime` | 较慢 |
| 1x 去压缩伪影 | `models/1xDeH264_RTMoSR.ncnn`（快）、`models/1x_DeH264_SPAN.ncnn`（低画质源） | 平台二压片源建议先跑 |
| 1x 去噪 | `models/dncnn_color_blind.pth.ncnn` | |
| 补帧（动画） | `models/rife-v4.26-heavy`（高质量）、`models/rife-v4.22` | |
| 补帧（通用/速度） | `models/rife-v4.26`、`models/rife-v4.6`（最快） | |

## 一条龙脚本（推荐日常使用）

`pipeline.sh`（仓库根目录，已实测通过）：破坏暗水印 → 自动选目标分辨率 → 超分 → 成品，一条命令完成。

```bash
./pipeline.sh input.mp4                          # 全自动（按短边选 720p/1080p/原尺寸增强）
./pipeline.sh input.mp4 out.mp4 720              # 强制输出 720p
./pipeline.sh input.mp4 out.mp4 auto 0.95 12     # 加强水印破坏（缩放95% + 平移±12px）
```

auto 规则：短边 <720 → 720p；720~1079 → 1080p；≥1080 → 原尺寸增强（DeH264 修复模型）。
竖屏自动保持宽高比（如 480x854 → 720x1280）。

> 已知问题：本机环境下 RVE 后端退出时 NCNN/Vulkan 清理阶段会报段错误（rc=139），此时输出已完整写入，脚本已自动容忍该退出码并校验输出文件，不影响结果。

## 注意事项

- Stage 1/2 的扰动强度与画质是 trade-off：`crf 23` 是文档实验后的较低检出率配置；想更低检出用 crf 25+ 或 pass2_scale 0.95，画质损耗增加。
- 中间产物经过两次 crf 23 重编码，细节已有损失——Stage 3 超分（生成式/模型超分）的作用正是补偿这部分损耗并重建纹理。
- 竖屏漫剧（如 720x1280）命令通用：Stage 2 用表达式自动适配；Stage 3 缩放时保持宽高比即可。
- 全流程：Stage 1/2 走 CPU（ffmpeg x264 veryslow，较慢），Stage 3 走 GPU（T4 上 NCNN 2x 超分约 30~40 FPS）。
